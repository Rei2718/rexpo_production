import { ThemedText } from "@/components/ui/themed-text";
import { ThemedView } from "@/components/ui/themed-view";

export default function TimeLineScreen() {
    return (
        <ThemedView style={{ flex: 1 }}>
            <ThemedText>Timeline</ThemedText>
        </ThemedView>
    );
}