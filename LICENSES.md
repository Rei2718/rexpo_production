# OSSライセンス情報の更新手順

本プロジェクトでは、`assets/licenses.json` を使用してアプリ内の「ライセンス」画面にOSSライセンス情報を表示しています。
新しいパッケージの追加や、既存パッケージの更新を行った際は、以下の手順でこのJSONファイルを更新してください。

## ライセンスリストの生成コマンド

プロジェクトのルートディレクトリで以下のコマンドを実行します。

```bash
npm run generate-licenses
```

## コマンドの仕様

このコマンド（`license-checker`）は以下の処理を行います：

1. `package.json` の `dependencies`（`devDependencies`を除く）をスキャンします。
2. Privateパッケージを除外します。
3. 各パッケージのライセンス情報、リポジトリURL、バージョンなどを抽出します。
4. 結果を `assets/licenses.json` に出力（上書き）します。

## 実行のタイミング

- 新しいライブラリを `npm install` した時
- ライブラリのバージョンを更新した時